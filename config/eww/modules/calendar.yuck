(defwindow calendar
    :monitor 0
    :stacking "fg"
    :exclusive true
    :namespace e_namespace

    :geometry (geometry :anchor "top right")
(w_calendar))

(defwidget w_calendar[]
(box :class "w_calendar"
    (calendar)
))
