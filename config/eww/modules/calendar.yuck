(defwindow calendar
    :monitor 0
    :stacking "fg"
    :namespace "eww_overlay"

    :geometry (geometry :anchor "top right")
(w_calendar))

(defwidget w_calendar[]
(box :class "w_calendar"
    (calendar)
))
